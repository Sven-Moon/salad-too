import * as fromPay from '../pay.reducer';
import { selectPayState } from '../pay.selectors';

// describe('Pay Selectors', () => {
//   it('should select the feature state', () => {
//     const result = selectPayState({
//       [fromPay.payFeatureKey]: {}
//     });

//     expect(result).toEqual({});
//   });
// });
